pluginManagement {
    repositories {
        google()
        // Copia di Maven Central su Google, provata per prima: Maven Central a volte limita le richieste.
        maven { url 'https://maven-central.storage-download.googleapis.com/maven2/' }
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositories {
        google()
        // Copia di Maven Central su Google, provata per prima: Maven Central a volte limita le richieste.
        maven { url 'https://maven-central.storage-download.googleapis.com/maven2/' }
        mavenCentral()
    }
}
rootProject.name = 'LettoreManoscritti'
include ':app'
